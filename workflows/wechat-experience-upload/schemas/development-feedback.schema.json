{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Workflow Development Feedback",
  "type": "object",
  "required": [
    "schema_version",
    "provider",
    "workspace",
    "feedback"
  ],
  "properties": {
    "schema_version": {
      "const": "workflow_runtime_result.v1"
    },
    "provider": {
      "type": "string",
      "minLength": 1
    },
    "workspace": {
      "type": "string",
      "minLength": 1
    },
    "feedback": {
      "type": "object",
      "required": [
        "decision",
        "reason"
      ],
      "properties": {
        "decision": {
          "enum": [
            "accepted",
            "rejected"
          ]
        },
        "reason": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true
}
